# Load a list of CmAnalysis from file

Load a list of objects of type `CmAnalysis` from file. The file is in
JSON format.

## Usage

``` r
loadCmAnalysisList(file)
```

## Arguments

- file:

  The name of the file

## Value

A list of objects of type `CmAnalysis`.
